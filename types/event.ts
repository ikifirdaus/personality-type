export interface CalendarEvent {
  id: string;
  title: string;
  date: string;
  description?: string;
  time: string; // Time part of the reservationDate
  status: string; // Time part of the reservationDate
}
